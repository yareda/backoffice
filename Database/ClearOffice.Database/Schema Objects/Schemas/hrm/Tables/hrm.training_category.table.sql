CREATE TABLE [hrm].[training_category] (
    [training_category_id]   INT            IDENTITY (1, 1) NOT NULL,
    [training_category_name] NVARCHAR (200) NULL,
    [description]            NVARCHAR (MAX) NULL
);

